export declare class ModuleResolver {
    private readonly presetCallerDirname;
    private readonly callerStrIndex;
    private readonly TS_EXT;
    private readonly JS_EXT;
    private readonly id;
    private filename;
    isSourceCode: boolean;
    constructor(id: string, presetCallerDirname?: string, depth?: number);
    getCallerDirname(): string;
    resolveModule(): string;
}
