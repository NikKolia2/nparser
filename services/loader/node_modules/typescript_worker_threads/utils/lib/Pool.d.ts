/// <reference types="node" />
import { EventEmitter } from 'events';
export declare class Pool extends EventEmitter {
    private readonly moduleResolver;
    private readonly id;
    private readonly size;
    private readonly queue;
    private readonly pool;
    private runningWorkers;
    private isReady;
    private closing;
    constructor(id: string, poolSize?: number, presetCallerDirname?: string, depth?: number);
    private sleep;
    private static handleErrors;
    private onRelease;
    private runWorker;
    private onMessage;
    private asyncAppendPoolWorker;
    private appendPoolWorker;
    getQueueLength(): number;
    getPoolLength(): number;
    run(workerData: any): Promise<unknown>;
    close(): void;
}
