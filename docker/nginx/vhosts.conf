server {
    listen 80;
    index index.php index.html;
    server_name api.santehnika-online.nikparser.ru;
    
    error_log  /parser/docker/nginx/logs/prod/error.log;
    access_log /parser/docker/nginx/logs/prod/access.log;

    root /parser/services/api/public;
    
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
    
    location / {
        try_files $uri $uri/ /index.php?$query_string;
        gzip_static on;
    }
}

server {
    listen 443 ssl;
    index index.php index.html;
    server_name api.vseinstruments.nikparser.ru;
    
    ssl_certificate /etc/letsencrypt/live/api.santehnika-online.nikparser.ru/fullchain.pem;
	ssl_certificate_key /etc/letsencrypt/live/api.santehnika-online.nikparser.ru/privkey.pem;
	
    error_log  /parser/docker/nginx/logs/prod/error.log;
    access_log /parser/docker/nginx/logs/prod/access.log;

    root /parser/services/api/public;
    
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
    
    location / {
        try_files $uri $uri/ /index.php?$query_string;
        gzip_static on;
    }
}